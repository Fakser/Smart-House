# flask_marshmallow

> Auto-generated documentation for [env.Lib.site-packages.flask_marshmallow](..\..\..\..\..\env\Lib\site-packages\flask_marshmallow\__init__.py) module.

Integrates the marshmallow serialization/deserialization library
with your Flask application.

- [Smart-house-rest-api](..\..\..\..\README.md#description) / [Modules](..\..\..\..\MODULES.md#smart-house-rest-api-modules) / `Env` / `Lib` / `Site-packages` / flask_marshmallow
    - [Marshmallow](#marshmallow)
        - [Marshmallow().init_app](#marshmallowinit_app)
    - Modules
        - [Compat](compat.md#compat)
        - [flask_marshmallow.fields](fields.md#flask_marshmallowfields)
        - [Schema](schema.md#schema)
        - [flask_marshmallow.sqla](sqla.md#flask_marshmallowsqla)

## Marshmallow

[[find in source code]](..\..\..\..\..\env\Lib\site-packages\flask_marshmallow\__init__.py#L51)

```python
class Marshmallow(object):
    def __init__(app=None):
```

Wrapper class that integrates Marshmallow with a Flask application.

To use it, instantiate with an application

```python
from flask import Flask

app = Flask(__name__)
ma = Marshmallow(app)
```

The object provides access to the class `Schema` class,
all fields in :mod:`marshmallow.fields`, as well as the Flask-specific
fields in :mod:`flask_marshmallow.fields`.

You can declare schema like so

```python
class BookSchema(ma.Schema):
    class Meta:
        fields = ('id', 'title', 'author', 'links')

    author = ma.Nested(AuthorSchema)

    links = ma.Hyperlinks({
        'self': ma.URLFor('book_detail', id='<id>'),
        'collection': ma.URLFor('book_list')
    })
```

In order to integrate with Flask-SQLAlchemy, this extension must be initialized *after*
`flask_sqlalchemy.SQLAlchemy`. 

```python
db = SQLAlchemy(app)
ma = Marshmallow(app)
```

This gives you access to `ma.SQLAlchemySchema` and `ma.SQLAlchemyAutoSchema`, which generate
marshmallow `~marshmallow.Schema` classes based on the passed in model or table. 

```python
class AuthorSchema(ma.SQLAlchemyAutoSchema):
    class Meta:
        model = Author
```

#### Arguments

- `app` *Flask* - The Flask application object.

### Marshmallow().init_app

[[find in source code]](..\..\..\..\..\env\Lib\site-packages\flask_marshmallow\__init__.py#L106)

```python
def init_app(app):
```

Initializes the application with the extension.

#### Arguments

- `app` *Flask* - The Flask application object.
